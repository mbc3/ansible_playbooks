ansible_become_password: "{{ vault_files_ansible_password }}"
caddy_services:
# key is subdomain, ie pihole.mediapi.localdomain
# value is port for reverse proxy, ie 8080
    rslsync: "8888"
    syncthing: "8384"
    torrent: "8080"
