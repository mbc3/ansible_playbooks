---
- name: Restart Home Assistant
  become: true
  become_user: "{{ home_assistant_user }}"
  ansible.builtin.systemd_service:
    name: home_assistant
    state: restarted
    scope: user
    daemon_reload: true
