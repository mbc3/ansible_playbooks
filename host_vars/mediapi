ansible_become_pass: "{{ mediapi_password }}"
caddy_services:
# key is subdomain, ie pihole.mediapi.localdomain
# value is port for reverse proxy, ie 8080
    books: "5000"
    pdf: "8080"
    syncthing: "8384"
